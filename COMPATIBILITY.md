# 兼容策略

## 版本线

言据库遵循语义化版本，`.yj` 格式另有独立的格式主版本。库 1.2.0 继续读写格式 v1，并兼容 1.0 与 1.1 的全部公共入口。

| 言据库 | `.yj` 格式 | 最低言序 | 状态 |
| --- | --- | --- | --- |
| 1.2.x | v1 | 1.1.6 | 当前稳定线 |
| 1.1.x | v1 | 1.1.5/1.1.6 | 仅维护严重问题 |
| 1.0.x | v1 | 1.0 | 历史稳定线 |

补丁版本不改变规范输出或公开签名。次版本可以增加 API、错误代码和可选规则；主版本才允许移除入口或改变既有语义。格式语法的破坏性变化还必须提升格式主版本和媒体类型 `version` 参数。

## 数据兼容

- 规范序列化按键稳定排序；同一值在同一格式主版本得到相同文本。
- 1.2 的显式资源限制不改变限制内文档的值语义。
- `差异` 产生的补丁只保证在相同格式主版本和匹配旧值上重放。
- TEXT/CLOB 使用 `.yj` 规范文本；PostgreSQL JSONB、MySQL JSON 和 SQLite JSON1 表示属于驱动层原生结构化策略，不能当作 `.yj` 原文。

## 平台

纯内存 API 在言序支持的 Windows、macOS 与 Linux 上行为一致。文件 API 使用 UTF-8，并受顶层应用的文件权限和各平台路径规则约束。
