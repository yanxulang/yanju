/// 言据核心路径的可重复微基准；输出为言据，便于保存和比较。
引「../src/言据.yx」为 言据；
引「标准:时间」为 时间；

令 条目：列 为【】；
令 次：数 为 0；
当 次 小于 5 则
    追加（条目，{
        「序号」：次，
        「名称」：「言据性能样本」，
        「启用」：次 小于 4，
        「标签」：【「配置」，「交换」，「中文」】，
        「参数」：{「重试」：3，「超时」：5000}
    }）；
    置 次 为 次 加 1；
终

定 样本：典 为 {「版本」：1，「条目」：条目}；
定 样本文：文 为 言据.序列化（样本）；
定 轮数：数 为 2；
定 查询次数：数 为 20；

定 解析始：数 为 时间.毫秒（）；
置 次 为 0；
当 次 小于 轮数 则
    言据.解析据（样本文）；
    置 次 为 次 加 1；
终
定 解析耗时：数 为 时间.毫秒（）减 解析始；

定 序列化始：数 为 时间.毫秒（）；
置 次 为 0；
当 次 小于 轮数 则
    言据.序列化（样本）；
    置 次 为 次 加 1；
终
定 序列化耗时：数 为 时间.毫秒（）减 序列化始；

定 查询始：数 为 时间.毫秒（）；
置 次 为 0；
当 次 小于 查询次数 则
    言据.查询文（样本，「条目／2／参数／超时」）；
    置 次 为 次 加 1；
终
定 查询耗时：数 为 时间.毫秒（）减 查询始；

言 言据.美化（{
    「样本字符数」：长度（样本文），
    「轮数」：轮数，
    「查询次数」：查询次数，
    「解析毫秒」：解析耗时，
    「序列化毫秒」：序列化耗时，
    「路径查询毫秒」：查询耗时
}）；
