引「错误.yx」为 错误；

引「路径.yx」为 路径；

引「标准:数学」为 数学；

公 法 深复制（值：数|文|理|空|列|典）：数|文|理|空|列|典 则
    若 （类型（值） 等于 「列」） 则
        令 结果：列 为 【】；
        逐 项 于 值 则
            追加（结果，深复制（项））；
        终
        归 结果；
    终
    若 （类型（值） 等于 「典」） 则
        令 结果：典 为 {}；
        逐 键 于 键列（值） 则
            置 结果【键】 为 深复制（值【键】）；
        终
        归 结果；
    终
    归 值；
终

公 法 相同（甲：数|文|理|空|列|典，乙：数|文|理|空|列|典）：理 则
    若 （类型（甲） 不等于 类型（乙）） 则
        归 假；
    终
    若 （类型（甲） 等于 「列」） 则
        若 （长度（甲） 不等于 长度（乙）） 则
            归 假；
        终
        令 次：数 为 0；
        当 （次 小于 长度（甲）） 则
            若 非 相同（甲【次】，乙【次】） 则
                归 假；
            终
            置 次 为 （次 加 1）；
        终
        归 真；
    终
    若 （类型（甲） 等于 「典」） 则
        若 （类型（乙） 等于 「典」） 则
            若 （长度（甲） 不等于 长度（乙）） 则
                归 假；
            终
            逐 键 于 键列（甲） 则
                若 非 有键（乙，键） 则
                    归 假；
                终
                若 非 相同（甲【键】，乙【键】） 则
                    归 假；
                终
            终
            归 真；
        终
        归 假；
    终
    归 （甲 等于 乙）；
终

法 列含（各项：列，目标：数|文|理|空|列|典）：理 则
    逐 项 于 各项 则
        若 相同（项，目标） 则
            归 真；
        终
    终
    归 假；
终

法 合并内（基础：数|文|理|空|列|典，覆盖：数|文|理|空|列|典，列策略：文）：数|文|理|空|列|典 则
    若 （类型（基础） 等于 「典」） 则
        若 （类型（覆盖） 等于 「典」） 则
            令 结果：典 为 {}；
            逐 键 于 键列（基础） 则
                置 结果【键】 为 深复制（基础【键】）；
            终
            逐 键 于 键列（覆盖） 则
                若 有键（结果，键） 则
                    置 结果【键】 为 合并内（结果【键】，覆盖【键】，列策略）；
                否则
                    置 结果【键】 为 深复制（覆盖【键】）；
                终
            终
            归 结果；
        终
    终
    若 （（类型（基础） 等于 「列」） 且 （类型（覆盖） 等于 「列」）） 则
        若 （列策略 等于 「替换」） 则
            归 深复制（覆盖）；
        终
        令 结果：列 为 【】；
        逐 项 于 基础 则
            追加（结果，深复制（项））；
        终
        逐 项 于 覆盖 则
            若 （（列策略 等于 「连接」） 或 非 列含（结果，项）） 则
                追加（结果，深复制（项））；
            终
        终
        归 结果；
    终
    归 深复制（覆盖）；
终

公 法 合并（基础：数|文|理|空|列|典，覆盖：数|文|理|空|列|典）：数|文|理|空|列|典 则
    归 合并内（基础，覆盖，「替换」）；
终

公 法 合并据（基础：典，覆盖：典）：典 则
    定 结果 为 合并内（基础，覆盖，「替换」）；
    若 （类型（结果） 等于 「典」） 则
        归 结果；
    终
    错误.抛出（「YJ_MERGE」，空，「」，「据合并产生了非据结果」）；
    归 {}；
终

公 法 合并为（基础：数|文|理|空|列|典，覆盖：数|文|理|空|列|典，列策略：文）：数|文|理|空|列|典 则
    若 （（（列策略 不等于 「替换」） 且 （列策略 不等于 「连接」）） 且 （列策略 不等于 「去重」）） 则
        错误.抛出（「YJ_MERGE」，空，「」，「列策略须为替换、连接或去重」）；
    终
    归 合并内（基础，覆盖，列策略）；
终

法 路副本（路径：列）：列 则
    令 结果：列 为 【】；
    逐 段 于 路径 则
        追加（结果，段）；
    终
    归 结果；
终

法 记差（结果：列，操作：文，路径：列，旧值：数|文|理|空|列|典，新值：数|文|理|空|列|典）：空 则
    追加（结果，{「操作」：操作，「路径」：路副本（路径），「旧值」：深复制（旧值），「新值」：深复制（新值）}）；
    归 空；
终

法 差异内（旧值：数|文|理|空|列|典，新值：数|文|理|空|列|典，路径：列，结果：列）：空 则
    若 相同（旧值，新值） 则
        归 空；
    终
    若 （类型（旧值） 等于 「典」） 则
        若 （类型（新值） 等于 「典」） 则
            逐 键 于 排序（键列（旧值）） 则
                若 非 有键（新值，键） 则
                    追加（路径，键）；
                    记差（结果，「删除」，路径，旧值【键】，空）；
                    弹出（路径）；
                终
            终
            逐 键 于 排序（键列（新值）） 则
                追加（路径，键）；
                若 有键（旧值，键） 则
                    差异内（旧值【键】，新值【键】，路径，结果）；
                否则
                    记差（结果，「新增」，路径，空，新值【键】）；
                终
                弹出（路径）；
            终
            归 空；
        终
    终
    若 （（类型（旧值） 等于 「列」） 且 （类型（新值） 等于 「列」）） 则
        定 共数：数 为 若小（长度（旧值），长度（新值））；
        令 次：数 为 0；
        当 （次 小于 共数） 则
            追加（路径，次）；
            差异内（旧值【次】，新值【次】，路径，结果）；
            弹出（路径）；
            置 次 为 （次 加 1）；
        终
        置 次 为 （长度（旧值） 减 1）；
        当 （次 不小于 共数） 则
            追加（路径，次）；
            记差（结果，「删除」，路径，旧值【次】，空）；
            弹出（路径）；
            置 次 为 （次 减 1）；
        终
        置 次 为 共数；
        当 （次 小于 长度（新值）） 则
            追加（路径，次）；
            记差（结果，「新增」，路径，空，新值【次】）；
            弹出（路径）；
            置 次 为 （次 加 1）；
        终
        归 空；
    终
    记差（结果，「修改」，路径，旧值，新值）；
    归 空；
终

法 若小（甲：数，乙：数）：数 则
    若 （甲 小于 乙） 则
        归 甲；
    否则
        归 乙；
    终
终

公 法 差异（旧值：数|文|理|空|列|典，新值：数|文|理|空|列|典）：列 则
    令 结果：列 为 【】；
    差异内（旧值，新值，【】，结果）；
    归 结果；
终

法 补丁错（代码：文，各段：列，消息：文）：空 则
    错误.抛出（代码，空，路径.呈现（各段），消息）；
终

法 下标（段，各段：列）：数 则
    若 （类型（段） 不等于 「数」） 则
        补丁错（「YJ_PATCH」，各段，「列路径段须为整数」）；
    终
    若 （（段 小于 0） 或 （数学.下取整（段） 不等于 段）） 则
        补丁错（「YJ_PATCH」，各段，「列路径段须为非负整数」）；
    终
    归 段；
终

法 删除路径内（当前：数|文|理|空|列|典，各段：列，位置：数）：数|文|理|空|列|典 则
    定 段 为 各段【位置】；
    定 是末：理 为 （位置 等于 （长度（各段） 减 1））；
    若 （类型（当前） 等于 「典」） 则
        若 （（类型（段） 不等于 「文」） 或 非 有键（当前，段）） 则
            补丁错（「YJ_PATCH_CONFLICT」，各段，「删除路径不存在」）；
        终
        令 结果：典 为 {}；
        逐 键 于 键列（当前） 则
            若 （键 不等于 段） 则
                置 结果【键】 为 深复制（当前【键】）；
            否则
                若 非 是末 则
                    置 结果【键】 为 删除路径内（当前【键】，各段，（位置 加 1））；
                终
            终
        终
        归 结果；
    终
    若 （类型（当前） 等于 「列」） 则
        定 序：数 为 下标（段，各段）；
        若 （序 不小于 长度（当前）） 则
            补丁错（「YJ_PATCH_CONFLICT」，各段，「删除下标不存在」）；
        终
        令 结果：列 为 【】；
        令 次：数 为 0；
        当 （次 小于 长度（当前）） 则
            若 （次 不等于 序） 则
                追加（结果，深复制（当前【次】））；
            否则
                若 非 是末 则
                    追加（结果，删除路径内（当前【次】，各段，（位置 加 1）））；
                终
            终
            置 次 为 （次 加 1）；
        终
        归 结果；
    终
    补丁错（「YJ_PATCH_CONFLICT」，各段，「删除路径经过非容器值」）；
    归 空；
终

法 须字段（项：典，名称：文，各段：列）：空 则
    若 非 有键（项，名称） 则
        补丁错（「YJ_PATCH」，各段，（「补丁项缺少字段：」 加 名称））；
    终
    归 空；
终

公 法 应用差异（基础：数|文|理|空|列|典，各差：列）：数|文|理|空|列|典 则
    令 结果 为 深复制（基础）；
    逐 原项 于 各差 则
        若 （类型（原项） 不等于 「典」） 则
            补丁错（「YJ_PATCH」，【】，「补丁项须为据」）；
        终
        定 项：典 为 原项；
        若 （非 有键（项，「路径」） 或 （类型（项【「路径」】） 不等于 「列」）） 则
            补丁错（「YJ_PATCH」，【】，「补丁路径须为列」）；
        终
        定 各段：列 为 项【「路径」】；
        须字段（项，「操作」，各段）；
        须字段（项，「旧值」，各段）；
        须字段（项，「新值」，各段）；
        若 （类型（项【「操作」】） 不等于 「文」） 则
            补丁错（「YJ_PATCH」，各段，「补丁操作须为文字」）；
        终
        定 操作：文 为 项【「操作」】；
        若 （操作 等于 「新增」） 则
            若 （长度（各段） 等于 0） 则
                补丁错（「YJ_PATCH」，各段，「不可新增空根路径」）；
            终
            若 路径.存在（结果，各段） 则
                补丁错（「YJ_PATCH_CONFLICT」，各段，「新增路径已经存在」）；
            终
            置 结果 为 路径.设定（结果，各段，项【「新值」】）；
        否则
            若 （操作 等于 「修改」） 则
                若 非 路径.存在（结果，各段） 则
                    补丁错（「YJ_PATCH_CONFLICT」，各段，「修改路径不存在」）；
                终
                若 非 相同（路径.查询（结果，各段），项【「旧值」】） 则
                    补丁错（「YJ_PATCH_CONFLICT」，各段，「修改前值与补丁不一致」）；
                终
                置 结果 为 路径.设定（结果，各段，项【「新值」】）；
            否则
                若 （操作 等于 「删除」） 则
                    若 （长度（各段） 等于 0） 则
                        补丁错（「YJ_PATCH」，各段，「不可删除根值」）；
                    终
                    若 非 路径.存在（结果，各段） 则
                        补丁错（「YJ_PATCH_CONFLICT」，各段，「删除路径不存在」）；
                    终
                    若 非 相同（路径.查询（结果，各段），项【「旧值」】） 则
                        补丁错（「YJ_PATCH_CONFLICT」，各段，「删除前值与补丁不一致」）；
                    终
                    置 结果 为 删除路径内（结果，各段，0）；
                否则
                    补丁错（「YJ_PATCH」，各段，「未知补丁操作」）；
                终
            终
        终
    终
    归 结果；
终

公 法 应用补丁（基础：数|文|理|空|列|典，各差：列）：数|文|理|空|列|典 则
    归 应用差异（基础，各差）；
终
