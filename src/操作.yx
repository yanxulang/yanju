/// 言据值的深复制、深比较、递归合并与差异计算。
引「错误.yx」为 错误；

#: 深复制言据值；所得容器与原容器互不共享可变节点。
公 法 深复制（值：数|文|理|空|列|典）：数|文|理|空|列|典 则
    若 类型（值）等于「列」则
        令 结果：列 为【】；
        逐 项 于 值 则 追加（结果，深复制（项））；终
        归 结果；
    终
    若 类型（值）等于「典」则
        令 结果：典 为{}；
        逐 键 于 键列（值）则
            置 结果【键】为 深复制（值【键】）；
        终
        归 结果；
    终
    归 值；
终

#: 与据键顺序无关的递归相等比较。
公 法 相同（甲：数|文|理|空|列|典，乙：数|文|理|空|列|典）：理 则
    若 类型（甲）不等于 类型（乙）则 归 假；终
    若 类型（甲）等于「列」则
        若 长度（甲）不等于 长度（乙）则 归 假；终
        令 次：数 为 0；
        当 次 小于 长度（甲）则
            若 非 相同（甲【次】，乙【次】）则 归 假；终
            置 次 为 次 加 1；
        终
        归 真；
    终
    若 类型（甲）等于「典」则
        若 类型（乙）等于「典」则
            若 长度（甲）不等于 长度（乙）则 归 假；终
            逐 键 于 键列（甲）则
                若 非 有键（乙，键）则 归 假；终
                若 非 相同（甲【键】，乙【键】）则 归 假；终
            终
            归 真；
        终
        归 假；
    终
    归 甲 等于 乙；
终

法 列含（各项：列，目标：数|文|理|空|列|典）：理 则
    逐 项 于 各项 则
        若 相同（项，目标）则 归 真；终
    终
    归 假；
终

法 合并内（基础：数|文|理|空|列|典，覆盖：数|文|理|空|列|典，列策略：文）：数|文|理|空|列|典 则
    若 类型（基础）等于「典」则
        若 类型（覆盖）等于「典」则
            令 结果：典 为{}；
            逐 键 于 键列（基础）则
                置 结果【键】为 深复制（基础【键】）；
            终
            逐 键 于 键列（覆盖）则
                若 有键（结果，键）则
                    置 结果【键】为 合并内（结果【键】，覆盖【键】，列策略）；
                否则
                    置 结果【键】为 深复制（覆盖【键】）；
                终
            终
            归 结果；
        终
    终
    若 类型（基础）等于「列」且 类型（覆盖）等于「列」则
        若 列策略 等于「替换」则 归 深复制（覆盖）；终
        令 结果：列 为【】；
        逐 项 于 基础 则 追加（结果，深复制（项））；终
        逐 项 于 覆盖 则
            若 列策略 等于「连接」或 非 列含（结果，项）则
                追加（结果，深复制（项））；
            终
        终
        归 结果；
    终
    归 深复制（覆盖）；
终

#: 递归合并两个值；据递归合并，列默认由覆盖值替换。
公 法 合并（基础：数|文|理|空|列|典，覆盖：数|文|理|空|列|典）：数|文|理|空|列|典 则
    归 合并内（基础，覆盖，「替换」）；
终

#: 递归合并两个据，并以明确的据类型返回。
公 法 合并据（基础：典，覆盖：典）：典 则
    定 结果 为 合并内（基础，覆盖，「替换」）；
    若 类型（结果）等于「典」则 归 结果；终
    错误.抛出（「YJ_MERGE」，空，「」，「据合并产生了非据结果」）；
    归{}；
终

#: 按 `替换`、`连接` 或 `去重` 策略递归合并列。
公 法 合并为（基础：数|文|理|空|列|典，覆盖：数|文|理|空|列|典，列策略：文）：数|文|理|空|列|典 则
    若 列策略 不等于「替换」且 列策略 不等于「连接」且 列策略 不等于「去重」则
        错误.抛出（「YJ_MERGE」，空，「」，「列策略须为替换、连接或去重」）；
    终
    归 合并内（基础，覆盖，列策略）；
终

法 路副本（路径：列）：列 则
    令 结果：列 为【】；
    逐 段 于 路径 则 追加（结果，段）；终
    归 结果；
终

法 记差（结果：列，操作：文，路径：列，旧值：数|文|理|空|列|典，新值：数|文|理|空|列|典）：空 则
    追加（结果，{
        「操作」：操作，
        「路径」：路副本（路径），
        「旧值」：深复制（旧值），
        「新值」：深复制（新值）
    }）；
    归 空；
终

法 差异内（旧值：数|文|理|空|列|典，新值：数|文|理|空|列|典，路径：列，结果：列）：空 则
    若 相同（旧值，新值）则 归 空；终
    若 类型（旧值）等于「典」则
        若 类型（新值）等于「典」则
            逐 键 于 排序（键列（旧值））则
                若 非 有键（新值，键）则
                    追加（路径，键）；
                    记差（结果，「删除」，路径，旧值【键】，空）；
                    弹出（路径）；
                终
            终
            逐 键 于 排序（键列（新值））则
                追加（路径，键）；
                若 有键（旧值，键）则
                    差异内（旧值【键】，新值【键】，路径，结果）；
                否则
                    记差（结果，「新增」，路径，空，新值【键】）；
                终
                弹出（路径）；
            终
            归 空；
        终
    终
    若 类型（旧值）等于「列」且 类型（新值）等于「列」则
        定 共数：数 为 若小（长度（旧值），长度（新值））；
        令 次：数 为 0；
        当 次 小于 共数 则
            追加（路径，次）；
            差异内（旧值【次】，新值【次】，路径，结果）；
            弹出（路径）；
            置 次 为 次 加 1；
        终
        置 次 为 长度（旧值）减 1；
        当 次 不小于 共数 则
            追加（路径，次）；
            记差（结果，「删除」，路径，旧值【次】，空）；
            弹出（路径）；
            置 次 为 次 减 1；
        终
        置 次 为 共数；
        当 次 小于 长度（新值）则
            追加（路径，次）；
            记差（结果，「新增」，路径，空，新值【次】）；
            弹出（路径）；
            置 次 为 次 加 1；
        终
        归 空；
    终
    记差（结果，「修改」，路径，旧值，新值）；
    归 空；
终

法 若小（甲：数，乙：数）：数 则
    若 甲 小于 乙 则 归 甲；否则 归 乙；终
终

#: 返回稳定排序的结构化差异列，每项含操作、路径、旧值和新值。
公 法 差异（旧值：数|文|理|空|列|典，新值：数|文|理|空|列|典）：列 则
    令 结果：列 为【】；
    差异内（旧值，新值，【】，结果）；
    归 结果；
终
