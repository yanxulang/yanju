/// 言据值的声明式类型与结构校验。
///
/// 规则本身也是言据：支持类型、枚举、数值范围、长度、列项目、据属性、
/// 必需键和额外键策略。检查结果是结构化错误列，不依赖异常控制流。
引「操作.yx」为 操作；
引「路径.yx」为 路径；
引「错误.yx」为 错误；
引「标准:数学」为 数学；

法 路副本（原路：列）：列 则
    令 结果：列 为【】；
    逐 段 于 原路 则 追加（结果，段）；终
    归 结果；
终

法 添错（结果：列，代码：文，原路：列，消息：文，预期：数|文|理|空|列|典，实际：数|文|理|空|列|典）：空 则
    追加（结果，{
        「代码」：代码，
        「路径」：路副本（原路），
        「路径文」：路径.呈现（原路），
        「消息」：消息，
        「预期」：预期，
        「实际」：实际
    }）；
    归 空；
终

法 类型名（值：数|文|理|空|列|典）：文 则
    若 类型（值）等于「典」则 归「据」；终
    归 类型（值）；
终

法 单类符合（值：数|文|理|空|列|典，名称：文）：理 则
    若 名称 等于「任意」则 归 真；终
    若 名称 等于「据」则 归 类型（值）等于「典」；终
    归 类型（值）等于 名称；
终

法 合法类型名（名称：文）：理 则
    归 名称 等于「任意」或 名称 等于「据」或 名称 等于「列」或 名称 等于「文」或 名称 等于「数」或 名称 等于「理」或 名称 等于「空」；
终

法 合法类型声明（所需）：理 则
    若 类型（所需）等于「文」则 归 合法类型名（所需）；终
    若 类型（所需）等于「列」则
        若 长度（所需）等于 0 则 归 假；终
        逐 名称 于 所需 则
            若 类型（名称）不等于「文」则 归 假；终
            若 非 合法类型名（名称）则 归 假；终
        终
        归 真；
    终
    归 假；
终

法 类型符合（值：数|文|理|空|列|典，所需）：理 则
    若 类型（所需）等于「文」则 归 单类符合（值，所需）；终
    若 类型（所需）等于「列」则
        逐 名称 于 所需 则
            若 类型（名称）等于「文」且 单类符合（值，名称）则 归 真；终
        终
    终
    归 假；
终

法 检查长度（值：数|文|理|空|列|典，规则：典，原路：列，结果：列）：空 则
    若 类型（值）不等于「文」且 类型（值）不等于「列」且 类型（值）不等于「典」则 归 空；终
    若 有键（规则，「最短」）则
        若 类型（规则【「最短」】）不等于「数」或 规则【「最短」】小于 0 或 数学.下取整（规则【「最短」】）不等于 规则【「最短」】则
            添错（结果，「YJ_SCHEMA」，原路，「最短须为非负整数」，「非负整数」，规则【「最短」】）；
        否则
            若 长度（值）小于 规则【「最短」】则
                添错（结果，「YJ_LENGTH_MIN」，原路，「长度小于下限」，规则【「最短」】，长度（值））；
            终
        终
    终
    若 有键（规则，「最长」）则
        若 类型（规则【「最长」】）不等于「数」或 规则【「最长」】小于 0 或 数学.下取整（规则【「最长」】）不等于 规则【「最长」】则
            添错（结果，「YJ_SCHEMA」，原路，「最长须为非负整数」，「非负整数」，规则【「最长」】）；
        否则
            若 长度（值）大于 规则【「最长」】则
                添错（结果，「YJ_LENGTH_MAX」，原路，「长度大于上限」，规则【「最长」】，长度（值））；
            终
        终
    终
    归 空；
终

法 检查数（值：数，规则：典，原路：列，结果：列）：空 则
    若 有键（规则，「最小」）则
        若 类型（规则【「最小」】）不等于「数」则
            添错（结果，「YJ_SCHEMA」，原路，「最小须为数」，「数」，规则【「最小」】）；
        否则
            若 值 小于 规则【「最小」】则
                添错（结果，「YJ_NUMBER_MIN」，原路，「数值小于下限」，规则【「最小」】，值）；
            终
        终
    终
    若 有键（规则，「最大」）则
        若 类型（规则【「最大」】）不等于「数」则
            添错（结果，「YJ_SCHEMA」，原路，「最大须为数」，「数」，规则【「最大」】）；
        否则
            若 值 大于 规则【「最大」】则
                添错（结果，「YJ_NUMBER_MAX」，原路，「数值大于上限」，规则【「最大」】，值）；
            终
        终
    终
    若 有键（规则，「整数」）则
        若 类型（规则【「整数」】）不等于「理」则
            添错（结果，「YJ_SCHEMA」，原路，「整数须为逻辑值」，「理」，规则【「整数」】）；
        否则
            若 规则【「整数」】且 数学.下取整（值）不等于 值 则
                添错（结果，「YJ_NUMBER_INTEGER」，原路，「须为整数」，真，值）；
            终
        终
    终
    归 空；
终

法 校验内（值：数|文|理|空|列|典，规则：典，原路：列，结果：列）：空 则
    若 有键（规则，「类型」）则
        定 所需 为 规则【「类型」】；
        若 非 合法类型声明（所需）则
            添错（结果，「YJ_SCHEMA」，原路，「类型须为已知类型名或非空类型名列」，「任意、据、列、文、数、理或空」，所需）；
            归 空；
        终
        若 非 类型符合（值，所需）则
            添错（结果，「YJ_TYPE」，原路，「值类型不符合规则」，所需，类型名（值））；
            归 空；
        终
    终

    若 有键（规则，「枚举」）则
        若 类型（规则【「枚举」】）不等于「列」则
            添错（结果，「YJ_SCHEMA」，原路，「枚举须为列」，「列」，规则【「枚举」】）；
        否则
            令 命中：理 为 假；
            逐 候选 于 规则【「枚举」】则
                若 操作.相同（值，候选）则 置 命中 为 真；终
            终
            若 非 命中 则 添错（结果，「YJ_ENUM」，原路，「值不在枚举中」，规则【「枚举」】，值）；终
        终
    终

    检查长度（值，规则，原路，结果）；
    若 类型（值）等于「数」则 检查数（值，规则，原路，结果）；终

    若 类型（值）等于「列」且 有键（规则，「项目」）则
        若 类型（规则【「项目」】）不等于「典」则
            添错（结果，「YJ_SCHEMA」，原路，「项目须为规则据」，「据」，规则【「项目」】）；
        否则
            令 次：数 为 0；
            当 次 小于 长度（值）则
                追加（原路，次）；
                校验内（值【次】，规则【「项目」】，原路，结果）；
                弹出（原路）；
                置 次 为 次 加 1；
            终
        终
    终

    若 类型（值）等于「典」则
        若 有键（规则，「允许额外」）且 类型（规则【「允许额外」】）不等于「理」则
            添错（结果，「YJ_SCHEMA」，原路，「允许额外须为逻辑值」，「理」，规则【「允许额外」】）；
        终
        若 有键（规则，「必需」）则
            若 类型（规则【「必需」】）不等于「列」则
                添错（结果，「YJ_SCHEMA」，原路，「必需须为文字列」，「列」，规则【「必需」】）；
            否则
                逐 键 于 规则【「必需」】则
                    若 类型（键）不等于「文」则
                        添错（结果，「YJ_SCHEMA」，原路，「必需项须为文字」，「文」，键）；
                    否则
                        若 非 有键（值，键）则
                            追加（原路，键）；
                            添错（结果，「YJ_REQUIRED」，原路，「缺少必需键」，真，空）；
                            弹出（原路）；
                        终
                    终
                终
            终
        终
        若 有键（规则，「属性」）则
            若 类型（规则【「属性」】）不等于「典」则
                添错（结果，「YJ_SCHEMA」，原路，「属性须为据」，「据」，规则【「属性」】）；
            否则
                定 属性：典 为 规则【「属性」】；
                逐 键 于 排序（键列（属性））则
                    若 有键（值，键）则
                        追加（原路，键）；
                        若 类型（属性【键】）等于「典」则
                            校验内（值【键】，属性【键】，原路，结果）；
                        否则
                            添错（结果，「YJ_SCHEMA」，原路，「属性规则须为据」，「据」，属性【键】）；
                        终
                        弹出（原路）；
                    终
                终
                若 有键（规则，「允许额外」）且 规则【「允许额外」】等于 假 则
                    逐 键 于 排序（键列（值））则
                        若 非 有键（属性，键）则
                            追加（原路，键）；
                            添错（结果，「YJ_EXTRA_KEY」，原路，「不允许额外键」，假，值【键】）；
                            弹出（原路）；
                        终
                    终
                终
            终
        否则
            若 有键（规则，「允许额外」）且 规则【「允许额外」】等于 假 则
                逐 键 于 排序（键列（值））则
                    追加（原路，键）；
                    添错（结果，「YJ_EXTRA_KEY」，原路，「不允许额外键」，假，值【键】）；
                    弹出（原路）；
                终
            终
        终
    终
    归 空；
终

#: 按规则检查值，返回全部结构化错误；空列表示通过。
公 法 检查（值：数|文|理|空|列|典，规则：典）：列 则
    令 结果：列 为【】；
    校验内（值，规则，【】，结果）；
    归 结果；
终

#: 返回值是否符合规则。
公 法 符合（值：数|文|理|空|列|典，规则：典）：理 则
    归 长度（检查（值，规则））等于 0；
终

#: 不符合时抛出第一项结构化校验错误。
公 法 断验（值：数|文|理|空|列|典，规则：典）：空 则
    定 各错：列 为 检查（值，规则）；
    若 长度（各错）大于 0 则
        定 首：典 为 各错【0】；
        错误.抛出（首【「代码」】，空，首【「路径文」】，首【「消息」】）；
    终
    归 空；
终
