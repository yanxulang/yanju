/// 言据 1.1 公共入口。
///
/// 保留 1.0 核心 API，并统一提供配置校验、路径、数据操作、格式转换、
/// 流式解析与结构化错误能力。
引「核心.yx」为 核心；

引「错误.yx」为 错误；

引「路径.yx」为 路径；

引「操作.yx」为 操作；

引「类型校验.yx」为 类型校验；

引「转换.yx」为 转换；

引「标准:文字」为 文字；

#: `.yj` 格式主版本；1.1 未改变格式 v1 的基础语义。
公 定 格式版本：数 为 核心.格式版本；

#: 言据标准库语义版本。
公 定 库版本：文 为 「1.1.0」；

#: 解析一个完整言据文档。
公 法 解析（原文：文）：数|文|理|空|列|典 则
    归 核心.解析（原文）；
终

#: 解析并要求根值为据。
公 法 解析据（原文：文）：典 则
    归 核心.解析据（原文）；
终

#: 解析并要求根值为列。
公 法 解析列（原文：文）：列 则
    归 核心.解析列（原文）；
终

#: 判断文字是否为完整合法的言据。
公 法 校验（原文：文）：理 则
    归 核心.校验（原文）；
终

#: 不抛错地尝试解析，返回 `{成功，值，错误}`。
公 法 尝试解析（原文：文）：典 则
    试 则
        归 错误.成功（核心.解析（原文））；
    救 所误 则
        归 错误.失败（所误）；
    终
终

#: 把捕获到的言序误值转换为结构化言据错误。
公 法 错误详情（所误：误）：典 则
    归 错误.详情（所误）；
终

#: 生成规范单行言据。
公 法 序列化（值：数|文|理|空|列|典）：文 则
    归 核心.序列化（值）；
终

#: 生成两空格缩进的多行言据。
公 法 美化（值：数|文|理|空|列|典）：文 则
    归 核心.美化（值）；
终

#: 以 0 至 8 个空格自定义缩进；0 为规范压缩形式。
公 法 美化为（值：数|文|理|空|列|典，宽：数）：文 则
    归 核心.美化为（值，宽）；
终

#: 解析后按给定缩进重新格式化。
公 法 格式化（原文：文，宽：数）：文 则
    归 核心.格式化（原文，宽）；
终

#: 压缩为规范单行言据。
公 法 压缩（原文：文）：文 则
    归 核心.压缩（原文）；
终

#: 读取 UTF-8 `.yj` 文件。
公 法 读取（路径：文）：数|文|理|空|列|典 则
    归 核心.读取（路径）；
终

#: 读取并要求根值为据。
公 法 读取据（路径：文）：典 则
    归 核心.读取据（路径）；
终

#: 读取并要求根值为列。
公 法 读取列（路径：文）：列 则
    归 核心.读取列（路径）；
终

#: 规范写入 `.yj` 文件。
公 法 写入（路径：文，值：数|文|理|空|列|典）：数 则
    归 核心.写入（路径，值）；
终

#: 美化写入 `.yj` 文件。
公 法 美写（路径：文，值：数|文|理|空|列|典）：数 则
    归 核心.美写（路径，值）；
终

#: 把全角斜线路径解析为段列。
公 法 路径分段（原文：文）：列<文> 则
    归 路径.分段（原文）；
终

#: 把路径段列呈现为规范文字路径。
公 法 路径呈现（各段：列）：文 则
    归 路径.呈现（各段）；
终

#: 按段列查询值。
公 法 查询（根：数|文|理|空|列|典，各段：列）：数|文|理|空|列|典 则
    归 路径.查询（根，各段）；
终

#: 按 `项目／能力／0` 形式查询值。
公 法 查询文（根：数|文|理|空|列|典，原文：文）：数|文|理|空|列|典 则
    归 路径.查询文（根，原文）；
终

#: 返回段列路径是否存在。
公 法 路径存在（根：数|文|理|空|列|典，各段：列）：理 则
    归 路径.存在（根，各段）；
终

#: 返回文字路径是否存在。
公 法 路径存在文（根：数|文|理|空|列|典，原文：文）：理 则
    归 路径.存在文（根，原文）；
终

#: 查询失败时返回默认值。
公 法 查询或（根：数|文|理|空|列|典，各段：列，默认：数|文|理|空|列|典）：数|文|理|空|列|典 则
    归 路径.查询或（根，各段，默认）；
终

#: 深复制后设定路径，不改写原值。
公 法 设定（根：数|文|理|空|列|典，各段：列，新值：数|文|理|空|列|典）：数|文|理|空|列|典 则
    归 路径.设定（根，各段，新值）；
终

#: 深复制据后设定路径，并以明确的据类型返回。
公 法 设定据（根：典，各段：列，新值：数|文|理|空|列|典）：典 则
    归 路径.设定据（根，各段，新值）；
终

#: 深复制言据值。
公 法 深复制（值：数|文|理|空|列|典）：数|文|理|空|列|典 则
    归 操作.深复制（值）；
终

#: 与据键顺序无关的深比较。
公 法 相同（甲：数|文|理|空|列|典，乙：数|文|理|空|列|典）：理 则
    归 操作.相同（甲，乙）；
终

#: 递归合并；列默认替换。
公 法 合并（基础：数|文|理|空|列|典，覆盖：数|文|理|空|列|典）：数|文|理|空|列|典 则
    归 操作.合并（基础，覆盖）；
终

#: 递归合并两个据，并以明确的据类型返回。
公 法 合并据（基础：典，覆盖：典）：典 则
    归 操作.合并据（基础，覆盖）；
终

#: 以替换、连接或去重策略合并列。
公 法 合并为（基础：数|文|理|空|列|典，覆盖：数|文|理|空|列|典，列策略：文）：数|文|理|空|列|典 则
    归 操作.合并为（基础，覆盖，列策略）；
终

#: 返回结构化深层差异列。
公 法 差异（旧值：数|文|理|空|列|典，新值：数|文|理|空|列|典）：列 则
    归 操作.差异（旧值，新值）；
终

#: 按言据规则检查值并返回全部错误。
公 法 检查类型（值：数|文|理|空|列|典，规则：典）：列 则
    归 类型校验.检查（值，规则）；
终

#: 返回值是否符合规则。
公 法 符合规则（值：数|文|理|空|列|典，规则：典）：理 则
    归 类型校验.符合（值，规则）；
终

#: 不符合规则时抛出第一项结构化错误。
公 法 断验（值：数|文|理|空|列|典，规则：典）：空 则
    归 类型校验.断验（值，规则）；
终

#: JSON 解析为言序值。
公 法 自JSON（原文：文）：数|文|理|空|列|典 则
    归 转换.自JSON（原文）；
终

#: JSON 解析为据；根值不是对象时抛出 `YJ_JSON`。
公 法 自JSON据（原文：文）：典 则
    归 转换.自JSON据（原文）；
终

#: JSON 解析为列；根值不是数组时抛出 `YJ_JSON`。
公 法 自JSON列（原文：文）：列 则
    归 转换.自JSON列（原文）；
终

#: 言序值序列化为 JSON。
公 法 转JSON（值：数|文|理|空|列|典）：文 则
    归 转换.转JSON（值）；
终

公 法 JSON转言据（原文：文）：文 则
    归 转换.JSON转言据（原文）；
终

公 法 言据转JSON（原文：文）：文 则
    归 转换.言据转JSON（原文）；
终

#: 配置型 TOML 解析为据。
公 法 自TOML（原文：文）：典 则
    归 转换.自TOML（原文）；
终

#: 据序列化为稳定配置型 TOML。
公 法 转TOML（值：典）：文 则
    归 转换.转TOML（值）；
终

公 法 TOML转言据（原文：文）：文 则
    归 转换.TOML转言据（原文）；
终

公 法 言据转TOML（原文：文）：文 则
    归 转换.言据转TOML（原文）；
终

/// 增量解析换行分帧的言据流；每个非空行是一个规范言据值。
公 类 流式解析器 则
    私 域 缓冲：文；
    私 域 行号：数；
    私 域 已结束：理；

    公 法 初始化（）则
        置 此.缓冲 为「」；
        置 此.行号 为 0；
        置 此.已结束 为 假；
    终

    私 法 解析行（原文：文）：数|文|理|空|列|典 则
        试 则
            归 核心.解析（文字.修剪（原文））；
        救 所误 则
            错误.抛出（「YJ_STREAM」，此.行号，「」，所误.消息）；
            归 空；
        终
    终

    #: 送入任意文字块，返回其中已经完整分帧的值。
    公 法 送入（分块：文）：列 则
        若 此.已结束 则 错误.抛出（「YJ_STREAM」，此.行号，「」，「流已经结束」）；终
        定 各行：列<文> 为 文字.分割（此.缓冲 加 分块，「\n」）；
        令 结果：列 为【】；
        令 次：数 为 0；
        当 次 小于 长度（各行）减 1 则
            置 此.行号 为 此.行号 加 1；
            若 文字.修剪（各行【次】）不等于「」则
                追加（结果，此.解析行（各行【次】））；
            终
            置 次 为 次 加 1；
        终
        置 此.缓冲 为 各行【长度（各行）减 1】；
        归 结果；
    终

    #: 结束输入并解析最后一个无换行帧。
    公 法 结束（）：列 则
        若 此.已结束 则 错误.抛出（「YJ_STREAM」，此.行号，「」，「流已经结束」）；终
        置 此.已结束 为 真；
        令 结果：列 为【】；
        若 文字.修剪（此.缓冲）不等于「」则
            置 此.行号 为 此.行号 加 1；
            追加（结果，此.解析行（此.缓冲））；
        终
        置 此.缓冲 为「」；
        归 结果；
    终
终

#: 一次解析完整的换行分帧言据流。
公 法 解析流（原文：文）：列 则
    定 所流：流式解析器 为 流式解析器（）；
    令 结果：列 为 所流.送入（原文）；
    逐 项 于 所流.结束（）则 追加（结果，项）；终
    归 结果；
终

#: 把值列序列化为以换行分帧的言据流。
公 法 序列化流（各值：列）：文 则
    令 各行：列<文> 为【】；
    逐 值 于 各值 则 追加（各行，核心.序列化（值））；终
    若 长度（各行）等于 0 则 归「」；终
    归 文字.联结（各行，「\n」）加「\n」；
终
