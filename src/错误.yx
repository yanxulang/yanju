/// 言据结构化错误协议。
///
/// 抛出消息采用 `代码|输入位置|数据路径|消息` 的稳定编码；文字字段使用
/// 百分号转义，因而路径或消息自身可以安全包含竖线。调用方可用 `详情`
/// 转为据，而无需解析面向人的文案。
引「标准:文字」为 文字；
引「标准:数学」为 数学；

法 编码字段（原文：文）：文 则
    定 百分号后：文 为 文字.替换（原文，「%」，「%25」）；
    归 文字.替换（百分号后，「|」，「%7C」）；
终

法 解码字段（原文：文）：文 则
    定 竖线后：文 为 文字.替换（原文，「%7C」，「|」）；
    归 文字.替换（竖线后，「%25」，「%」）；
终

法 数码文（值：数）：文 则
    若 值 等于 0 则 归「0」；终
    若 值 等于 1 则 归「1」；终
    若 值 等于 2 则 归「2」；终
    若 值 等于 3 则 归「3」；终
    若 值 等于 4 则 归「4」；终
    若 值 等于 5 则 归「5」；终
    若 值 等于 6 则 归「6」；终
    若 值 等于 7 则 归「7」；终
    若 值 等于 8 则 归「8」；终
    若 值 等于 9 则 归「9」；终
    抛「YJ_ERROR|||错误位置含非法数码」；
终

法 整数文（值：数）：文 则
    若 值 等于 0 则 归「0」；终
    令 余值：数 为 值；
    令 片：列<文> 为【】；
    当 余值 大于 0 则
        定 商：数 为 数学下整（余值 除 10）；
        追加（片，数码文（余值 减 商 乘 10））；
        置 余值 为 商；
    终
    归 文字.联结（反转（片），「」）；
终

法 数学下整（值：数）：数 则
    归 数学.下取整（值）；
终

法 数码值（字：文）：数 则
    若 字 等于「0」则 归 0；终
    若 字 等于「1」则 归 1；终
    若 字 等于「2」则 归 2；终
    若 字 等于「3」则 归 3；终
    若 字 等于「4」则 归 4；终
    若 字 等于「5」则 归 5；终
    若 字 等于「6」则 归 6；终
    若 字 等于「7」则 归 7；终
    若 字 等于「8」则 归 8；终
    若 字 等于「9」则 归 9；终
    归 -1；
终

法 位置值（文字值：文）：数? 则
    若 长度（文字值）等于 0 则 归 空；终
    令 结果：数 为 0；
    逐 字：文 于 文字.字符列（文字值）则
        定 数码：数 为 数码值（字）；
        若 数码 小于 0 则 归 空；终
        置 结果 为 结果 乘 10 加 数码；
    终
    归 结果；
终

#: 以稳定结构编码并抛出言据错误。
公 法 抛出（代码：文，输入位置：数?，数据路径：文，消息：文）：空 则
    令 位置文：文 为「」；
    若 输入位置 不等于 空 则
        置 位置文 为 整数文（输入位置）；
    终
    抛 代码 加「|」加 位置文 加「|」加 编码字段（数据路径）加「|」加 编码字段（消息）；
终

#: 把言序误值转换为稳定的结构化错误据。
公 法 详情（所误：误）：典 则
    定 各段：列<文> 为 文字.分割（所误.消息，「|」）；
    令 代码：文 为「YJ_RUNTIME」；
    令 输入位置：数? 为 空；
    令 数据路径：文 为「」；
    令 消息：文 为 所误.消息；
    若 长度（各段）不小于 4 且 文字.始于（各段【0】，「YJ_」）则
        置 代码 为 各段【0】；
        置 输入位置 为 位置值（各段【1】）；
        置 数据路径 为 解码字段（各段【2】）；
        置 消息 为 解码字段（文字.联结（各段【3：】，「|」））；
    终
    归 {
        「代码」：代码，
        「消息」：消息，
        「输入位置」：输入位置，
        「数据路径」：数据路径，
        「源位置」：所误.位置，
        「踪迹」：所误.踪迹
    }；
终

#: 构造不抛错 API 的成功结果。
公 法 成功（值：数|文|理|空|列|典）：典 则
    归 {「成功」：真，「值」：值，「错误」：空}；
终

#: 构造不抛错 API 的失败结果。
公 法 失败（所误：误）：典 则
    归 {「成功」：假，「值」：空，「错误」：详情（所误）}；
终
