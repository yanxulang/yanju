引「../src/言据.yx」为 言据；
引「标准:测试」为 测试；

定 配置：典 为 {
    「服务」：{「主机」：「127.0.0.1」，「端口」：8080}，
    「标签」：【「正式」，「中文」】，
    「含／斜」：7
}；

定 规则：典 为 {
    「类型」：「据」，
    「必需」：【「服务」，「标签」】，
    「允许额外」：假，
    「属性」：{
        「服务」：{
            「类型」：「据」，
            「必需」：【「主机」，「端口」】，
            「允许额外」：假，
            「属性」：{
                「主机」：{「类型」：「文」，「最短」：1}，
                「端口」：{「类型」：「数」，「最小」：1，「最大」：65535，「整数」：真}
            }
        }，
        「标签」：{「类型」：「列」，「最短」：1，「项目」：{「类型」：「文」，「最短」：2}}，
        「含／斜」：{「类型」：「数」}
    }
}；

测试.断言（言据.符合规则（配置，规则），「合法配置应通过」）；
测试.相等（长度（言据.检查类型（配置，规则）），0）；
测试.相等（言据.查询文（配置，「服务／端口」），8080）；
测试.相等（言据.查询（配置，【「标签」，1】），「中文」）；
测试.相等（言据.查询文（配置，「含＼／斜」），7）；
测试.断言（言据.路径存在文（配置，「服务／主机」），「主机路径应存在」）；
测试.断言（非 言据.路径存在文（配置，「服务／用户」），「用户路径不应存在」）；
测试.相等（言据.查询或（配置，【「服务」，「用户」】，「无人」），「无人」）；

定 新配置：典 为 言据.设定据（配置，【「服务」，「端口」】，9090）；
测试.相等（言据.查询文（新配置，「服务／端口」），9090）；
测试.相等（言据.查询文（配置，「服务／端口」），8080）；

定 坏配置：典 为 {
    「服务」：{「主机」：「」，「端口」：70000，「额外」：真}，
    「标签」：【「短」】，
    「另外」：1
}；
定 各错：列 为 言据.检查类型（坏配置，规则）；
测试.断言（长度（各错）不小于 4，「应收集范围、长度和额外键错误」）；
测试.断言（非 言据.符合规则（坏配置，规则），「坏配置不应通过」）；

定 坏规则错：列 为 言据.检查类型（1，{「类型」：「整数」}）；
测试.相等（坏规则错【0】【「代码」】，「YJ_SCHEMA」）；
