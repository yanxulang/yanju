引「../src/言据.yx」为 言据；

引「标准:测试」为 测试；

定 默认：典 为 言据.默认限制（）；

测试.断言（（默认【「最大字符」】 大于 0），「默认字符上限须为正数」）；

测试.断言（（默认【「最大深度」】 大于 0），「默认深度上限须为正数」）；

测试.断言（（默认【「最大容器项」】 大于 0），「默认容器项上限须为正数」）；

定 小限制：典 为 {「最大字符」：32，「最大深度」：2，「最大容器项」：3}；

测试.断言（言据.相同（言据.解析受限（「列【据【「值\」：1】】」，小限制），【{「值」：1}】），「限制内文档应成功」）；

法 须报（原文：文，限制：典，代码：文）：空 则
    令 已报：理 为 假；
    试 则
        言据.解析受限（原文，限制）；
    救 所误 则
        置 已报 为 真；
        测试.相等（言据.错误详情（所误）【「代码」】，代码）；
    终
    测试.断言（已报，「超限输入必须失败」）；
    归 空；
终

须报（「列【1】」，{「最大字符」：3}，「YJ_SIZE」）；

须报（「列【列【1】】」，{「最大深度」：1}，「YJ_DEPTH」）；

须报（「列【1，2】」，{「最大容器项」：1}，「YJ_ITEMS」）；

测试.断言（非 言据.校验受限（「列【1，2】」，{「最大容器项」：1}），「受限校验应返回假」）；

定 所流 为 言据.流式解析器（）.限制（{「最大字符」：8}）；

令 流已报：理 为 假；

试 则
    所流.送入（「据【「长名字\」：1」）；
救 所误 则
    置 流已报 为 真；
    测试.相等（言据.错误详情（所误）【「代码」】，「YJ_SIZE」）；
终

测试.断言（流已报，「未终止的超大帧必须立即失败」）；

令 配置已报：理 为 假；

试 则
    言据.规范限制（{「最大深度」：0}）；
救 所误 则
    置 配置已报 为 真；
    测试.相等（言据.错误详情（所误）【「代码」】，「YJ_LIMIT_CONFIG」）；
终

测试.断言（配置已报，「非法限制配置必须失败」）；
