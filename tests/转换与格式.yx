引「../src/言据.yx」为 言据；
引「标准:测试」为 测试；

定 原文：文 为 "据【\n 「乙」：列【1，2】，\n 「甲」：真\n】"；
定 压后：文 为 言据.压缩（原文）；
测试.相等（压后，"据【「乙」：列【1，2】，「甲」：真】"）；
测试.相等（言据.格式化（原文，0），压后）；
测试.相等（言据.格式化（原文，4），"据【\n    「乙」： 列【\n        1，\n        2\n    】，\n    「甲」： 真\n】"）；

定 JSON文：文 为 "{\"name\":\"言据\",\"enabled\":true,\"ports\":[80,443]}"；
定 JSON值：典 为 言据.自JSON据（JSON文）；
测试.相等（JSON值【「name」】，「言据」）；
测试.断言（言据.相同（JSON值，言据.解析据（言据.JSON转言据（JSON文））），「JSON 转言据应保持值」）；
测试.断言（言据.相同（JSON值，言据.自JSON（言据.言据转JSON（言据.序列化（JSON值）））），「言据转 JSON 应保持值」）；

定 TOML文：文 为 "\"标题\" = \"言据\"\n\"启用\" = true\n\"端口\" = [80, 443]\n\n[\"服务\"]\n\"主机\" = \"127.0.0.1\"\n\"选项\" = { \"重试\" = 3, \"区域\" = \"中国\" }\n"；
定 TOML值：典 为 言据.自TOML（TOML文）；
测试.相等（TOML值【「标题」】，「言据」）；
测试.相等（言据.查询文（TOML值，「服务／选项／重试」），3）；
定 TOML往返：典 为 言据.自TOML（言据.转TOML（TOML值））；
测试.断言（言据.相同（TOML值，TOML往返），「TOML 往返应保持配置」）；
测试.断言（言据.相同（TOML值，言据.解析据（言据.TOML转言据（TOML文））），「TOML 转言据应保持配置」）；
