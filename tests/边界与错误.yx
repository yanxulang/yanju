引「../src/言据.yx」为 言据；

引「标准:测试」为 测试；

测试.相等（言据.序列化（言据.解析（「据【】」）），「据【】」）；

测试.相等（言据.序列化（言据.解析（「列【】」）），「列【】」）；

测试.相等（言据.解析（「-12.5」），减 12.5）；

测试.相等（言据.解析（「0.000000000000001」），0.000000000000001）；

测试.相等（言据.序列化（0.333333333333333），「0.333333333333333」）；

测试.断言（非 言据.校验（「」），「空文档须失败」）；

测试.断言（非 言据.校验（「01」），「前导零须失败」）；

测试.断言（非 言据.校验（「1.」），「空小数部分须失败」）；

测试.断言（非 言据.校验（「9007199254740992」），「超过安全范围须失败」）；

测试.断言（非 言据.校验（「0.0000000000000001」），「过小非零值须失败」）；

测试.断言（非 言据.校验（「真 假」），「根值后内容须失败」）；

测试.断言（非 言据.校验（「列【1，】」），「尾随逗号须失败」）；

测试.断言（非 言据.校验（「列【1,2】」），「半角逗号须失败」）；

测试.断言（非 言据.校验（「据【「甲\」：1，「甲\」：2】」），「重复键须失败」）；

测试.断言（非 言据.校验（「据【甲：1】」），「据键未加引号须失败」）；

测试.断言（非 言据.校验（「「未知\\错转义\」」），「未知转义须失败」）；
